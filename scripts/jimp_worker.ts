// "jimp": "^0.22.8",

// import jimp from "jimp";

// class Circle {
//     radius: number;
//     x: number;
//     y: number;

//     constructor(radius:number, x:number, y:number) {
//         this.radius = radius;
//         this.x = x;
//         this.y = y;
//     }
// }

// async function main(image_path: string, cutout: Circle) {
//     const image = await jimp.read(image_path);

//     image.circle(cutout)
// }

// main('', new Circle(50,25,25))